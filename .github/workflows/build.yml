name: Build TravelWizards.exe

on:
  push:
    branches: [main]
  workflow_dispatch:

jobs:
  build:
    name: Build — TravelWizards.exe
    runs-on: windows-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up Python 3.11
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: Install dependencies
        working-directory: hotel_invoice_processor
        run: pip install -r requirements.txt

      - name: Build executable
        working-directory: hotel_invoice_processor
        run: pyinstaller portal.spec

      - name: Zip executable
        run: |
          cd hotel_invoice_processor/dist
          Compress-Archive -Path TravelWizards.exe -DestinationPath TravelWizards.zip

      - name: Upload artifact
        uses: actions/upload-artifact@v4
        with:
          name: TravelWizards-windows
          path: hotel_invoice_processor/dist/TravelWizards.zip
          if-no-files-found: error

  release:
    name: Create Release
    needs: build
    runs-on: ubuntu-latest
    if: github.event_name == 'push' && github.ref == 'refs/heads/main'

    permissions:
      contents: write

    steps:
      - name: Download artifact
        uses: actions/download-artifact@v4
        with:
          name: TravelWizards-windows
          path: dist

      - name: Create Release
        uses: softprops/action-gh-release@v2
        with:
          tag_name: build-${{ github.run_number }}
          name: "Build ${{ github.run_number }}"
          body: |
            Automated build — commit ${{ github.sha }}

            **Download TravelWizards.zip**, unzip, run `TravelWizards.exe`.
            No installation required.
          files: dist/TravelWizards.zip
          draft: false
          prerelease: false
